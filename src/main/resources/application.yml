app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/postgres
      password: 'password'
      pool-size: 30
      username: chinganshih

server:
  error:
    include-binding-errors: always
    include-message: always


